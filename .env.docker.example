# ============================================
# Variables de Entorno - Contenedor Docker MySQL (EJEMPLO)
# ============================================
#
# Copia este archivo como .env.docker y completa con tus valores reales:
#   cp .env.docker.example .env.docker
#
# IMPORTANTE: .env.docker NO se sube a Git. Solo este archivo de ejemplo.
#

MYSQL_ROOT_PASSWORD=cambia_esto_por_una_password_segura
MYSQL_DATABASE=pps_db
MYSQL_USER=cambia_esto_por_el_usuario
MYSQL_PASSWORD=cambia_esto_por_una_password_segura

